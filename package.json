{
  "name": "angelette-home",
  "version": "1.0.0",
  "description": "Site da ANGELETI INCORPORADORA - HTML, CSS e JavaScript puro",
  "private": true,
  "scripts": {
    "serve": "npx http-server . -p 3000 -o",
    "build": "mkdir -p dist && cp -r *.html pages css js public netlify.toml dist/ 2>/dev/null || echo 'Files copied'"
  },
  "devDependencies": {}
}
